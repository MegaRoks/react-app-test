import React, { Fragment } from 'react';

import { Form } from './../components/Form.component';

export const Home = () => {
    return (
        <Fragment>
            <Form />
        </Fragment>
    );
};
